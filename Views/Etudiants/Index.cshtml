@model IEnumerable<Etudiant>
@{
    ViewData["Title"] = "Étudiants";
}

<section class="page-header page-header-inline">
    <div><h1>Étudiants</h1></div>
    <a asp-action="Create" class="btn btn-primary">Nouvel étudiant</a>
</section>

<div class="panel-card">
    <div class="table-responsive">
        <table class="table align-middle">
            <thead>
                <tr>
                    <th>Matricule</th>
                    <th>Nom</th>
                    <th>Référentiel</th>
                    <th>Absences</th>
                    <th>Compte</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Matricule</td>
                        <td>@item.NomComplet</td>
                        <td>@item.Referentiel?.Libelle</td>
                        <td>@item.Absences.Count</td>
                        <td>@(item.Compte?.Email ?? "Aucun")</td>
                        <td class="text-end">
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-dark">Modifier</a>
                            <form asp-action="Delete" asp-route-id="@item.Id" method="post" class="d-inline">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-sm btn-outline-danger">Supprimer</button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
